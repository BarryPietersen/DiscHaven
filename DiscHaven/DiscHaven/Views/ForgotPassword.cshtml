@*using your libraries*@


@{
    if (Request["password"] != null && Request["confirmPassword"] != null && (bool)Session["validated"])
    {
        //perform password validation and matches here
        //update the database using the Session["ID"]
        //display successful message
    }
    else if (Request["answer1"] != null && Request["answer2"] != null && Session["ID"] != null)
    {
        //using the session["ID"] stored and two answers
        //hash and compare these to the records in the database
        //if matches are found store a bool on the session - Session["validated"] = true
        //display readonly input field containing the username value
        //and the password/confirm boxes
    }
    else if (Request["username"] != null)
    {
        //check the database for a match,
        //store the matching ID on session
        //retirve and display the challenge QAs with a hidden ID field,
        //and readonly input field containing the username value
        //matching the QA records
    }
    else if (Request["username"] == null)
    {
        //display a simple username text box and maybe
        //some other user guide information
    }
 }